#!/usr/bin/env sh

test "${CONTAINER_IMAGE}" = "" && CONTAINER_IMAGE="elm-lmp-web:local"

docker build -t ${CONTAINER_IMAGE} .
docker build -t ${CONTAINER_IMAGE}-development --target=development .
docker build -t ${CONTAINER_IMAGE}-pa11y --target=pa11y .
